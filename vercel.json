{
  "buildCommand": "yarn playwright install && yarn ci-test",
  "outputDirectory": "actionkit/reports/html_report",
  "installCommand": "yarn install",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
