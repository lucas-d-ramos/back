# ActionKit Tests

Automatic frontend tests for Actionkit website, using BackstopJS (and Playwright) for browser puppeting test.

If netifly gives you problems while installing run this:
`yarn install --ignore-optional`
